
{% block title %}Black Dog Education - Erreur {{ exception.statusCode }}
{% endblock %}

{% block body %}
	<div class="container-fluid bg-light overflow-hidden px-lg-0 py-3">
		<div class="container col-lg-6 py-5 d-flex justify-content-center align-items-center">
        <h1 class="h3 mb-3 font-weight-normal text-center">HTTP Error {{ exception.statusCode }}</h1>

      <p class="my-2 text-2xl font-bold text-center text-gray-800 md:text-3xl">
        {{ exception.statusText }}
      </p>

      <a
        href="{{ path('app_index') }}"
        class="px-6 py-2 mt-6 text-sm font-semibold text-blue-800 bg-blue-100 text-center"
        >Retourner à l'accueil</a
      >
		</div>
	</div>
{% endblock %}
